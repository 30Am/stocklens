[build]
builder = "nixpacks"

[deploy]
startCommand = "/opt/venv/bin/uvicorn app.main:app --host 0.0.0.0 --port $PORT --workers 1 --ws websockets --no-access-log"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[environments.production.variables]
ENVIRONMENT = "production"
LOG_LEVEL = "INFO"
TZ = "Asia/Kolkata"
